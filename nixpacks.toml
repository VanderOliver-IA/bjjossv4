# Nixpacks configuration for Vite/React
[phases.setup]
nixPkgs = ["nodePackages.npm", "nodejs-20_x"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm run preview -- --host 0.0.0.0 --port 3000"
